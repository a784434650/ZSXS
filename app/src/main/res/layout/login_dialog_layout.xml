<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="180dp"
              android:layout_height="120dp"
              android:background="@mipmap/article_net_set_back"
              android:paddingBottom="10.0dip">
    <ProgressBar
        android:id="@+id/pg_login_dialog"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:indeterminateDrawable="@drawable/progressbar_circle" />

    <Button
        android:layout_alignParentBottom="true"
        android:textStyle="bold"
            android:textColor="#ff99dd8b"
            android:id="@+id/ib_cancel"
            android:background="#66cccccc"
            android:padding="10.0dip"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10.0dip"
            android:layout_marginTop="15.0dip"
            android:layout_marginRight="10.0dip"
            android:text="取消" />
    <TextView
        android:id="@+id/tv_login_content"
        android:layout_below="@id/pg_login_dialog"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="登录中"/>

</RelativeLayout>